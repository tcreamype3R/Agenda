{
  "name": "agenda-lecturas-biblicas",
  "version": "1.0.0",
  "description": "App para gestionar lecturas bíblicas con Firebase y Vercel",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel start",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase": "^9.6.0",
    "firebase-admin": "^10.0.2",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "vercel": "^28.4.0"
  },
  "engines": {
    "node": ">=14.x"
  }
}
